---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  canonical?: string;
  schema?: Record<string, any>;
}

const { 
  title, 
  description = "Done-for-you AI workflows for founders & lean teams. Save 10–20+ hours weekly with Make.com, n8n, Zapier & OpenAI.",
  canonical,
  schema
} = Astro.props;
---

<!doctype html>
<html lang="en" class="dark">
  <head>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-48DL7G7HTK"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-48DL7G7HTK');
    </script>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/png" href="/favicon.png">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description}>
    <meta name="author" content="Saad Rasheed" />
    <meta name="google-site-verification" content="W_u2fBNjH_rWtC6QNjs2JhiZ8fu4zEbhUC694Z6-MzY" />
    <link rel="alternate" type="text/markdown" href="/llms.txt" title="llms.txt" />
    <link rel="alternate" type="text/markdown" href="/llms-full.txt" title="llms-full.txt" />
    {canonical && <link rel="canonical" href={canonical} />}
    {schema && <script type="application/ld+json" set:html={JSON.stringify(schema)} />}

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Saad Rasheed | AI Calling Agents for Real Estate" />
    {canonical && <meta property="og:url" content={canonical} />}
    <meta name="twitter:card" content="summary_large_image" />
    <meta property="og:title" content={title}>
    <meta name="twitter:title" content={title}>
    <meta property="og:description" content={description}>
    <meta name="twitter:description" content={description}>
    <meta property="og:image" content="https://saadrasheed.life/og-image.png">
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="Saad Rasheed — Done-For-You AI Calling Agents for Real Estate Teams" />
    <meta name="twitter:image" content="https://saadrasheed.life/og-image.png">
  </head>
  <body class="bg-background">
    <slot />
    <script>
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("is-visible");
              observer.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.12, rootMargin: "0px 0px -60px 0px" }
      );
      document.querySelectorAll(".fade-in-up").forEach((el) => observer.observe(el));
    </script>
    <script is:inline>
      // WebMCP Integration
      if (typeof navigator !== "undefined" && navigator.modelContext) {
        navigator.modelContext.provideContext({
          tools: [
            {
              name: "browse_projects",
              description: "Browse deployed Real Estate AI calling agents and workflow automation systems built by Saad Rasheed",
              inputSchema: {
                type: "object",
                properties: {
                  category: { type: "string", description: "Optional filter by real estate or operations automation" }
                }
              },
              execute: async (input) => {
                return {
                  text: "You can explore Saad Rasheed's real estate AI calling agents, speed-to-lead automation systems, and case studies at https://saadrasheed.life/projects or test a live voice demo at https://saadrasheed.life/demo"
                };
              }
            },
            {
              name: "book_strategy_call",
              description: "Book a free 30-minute Strategy Call with Saad Rasheed to map out custom AI calling workflows",
              inputSchema: {
                type: "object",
                properties: {}
              },
              execute: async (input) => {
                return {
                  text: "Book a free strategy call directly on Saad Rasheed's calendar at https://cal.com/saadrasheed/free-strategy-call"
                };
              }
            }
          ]
        });
      }
    </script>
  </body>
</html>
